services:
  - type: web
    name: speech-video-to-transcript
    env: python
    branch: main
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT
    plan: free
